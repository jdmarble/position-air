---
kind: role
version: v7
metadata:
  name: kube-system-master
spec:
  allow:
    kubernetes_labels:
      "*": "*"
    kubernetes_groups: ["system:masters"]
---
kind: user
version: v2
metadata:
  name: "{{ teleport_cluster_admin_username }}"
spec:
  roles:
    - kube-system-master
